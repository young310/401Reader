# .env.example
# 複製此檔案為 .env 並填入實際值

# === Flask 配置 ===
FLASK_ENV=development
SECRET_KEY=your-secret-key-change-in-production

# === 資料庫配置 ===
DB_USER=taxai
DB_PASSWORD=taxai_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=taxai
# 或使用完整 DATABASE_URL：
# DATABASE_URL=postgresql://taxai:taxai_password@localhost:5432/taxai

# === Celery/Redis 配置 ===
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# === 檔案上傳配置 ===
TAX_OCR_UPLOAD_BASE=/path/to/uploads

# === Azure Document Intelligence（OCR 服務）===
AZURE_DI_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
AZURE_DI_KEY=your-azure-di-key
AZURE_DI_MODEL=prebuilt-invoice

# === Azure OpenAI 配置 ===
AZURE_OPENAI_KEY=your-azure-openai-key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-12-01-preview
LLM_MODEL=gpt-4.1-mini
LLM_PROVIDER=azure_openai

# === JWT 配置 ===
JWT_SECRET_KEY=your-jwt-secret-key
JWT_EXPIRES_HOURS=24
